baseURL = 'https://cross-entropy-ai.github.io/'
languageCode = 'en-us'
title = 'Cross-entropy AI'
theme = 'crossentropy'
summaryLength = 40

[params]
  description = 'Reforge the future of AI'

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
  [markup.highlight]
    style = 'github'
    lineNos = false
